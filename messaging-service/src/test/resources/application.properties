#Database
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.jpa.hibernate.ddl-auto=create
spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect

#GraphQL
graphql.servlet.maxQueryDepth=5
graphql.servlet.tracingEnabled=true
voyager.cdn.enabled=true

# Authentication
app.auth.jwtSecret=${JWT_SECRET}
app.auth.jwtExpirationInMs=9999999
app.auth.auth-free.urls=/api/v1/auth/**,/playground,/vendor/**
app.auth.groups-permitted=NOTIFICATION_FORGE