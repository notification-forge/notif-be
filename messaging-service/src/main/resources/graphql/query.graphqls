type Query {
    template(templateId: ID): Template
    templates(searchValue: String, appCodes: [String], limit: Int!, after: Int): TemplateConnection @connection (for: "Template")
    templateVersion(templateVersionId: ID): TemplateVersion
    images(appCode: String, limit: Int!, after: Int): ImageConnection @connection (for: "Image")
}

